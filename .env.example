# Mailchimp configuration (server-side only)
# Find your server prefix (dc) in your API key suffix, e.g., us21
MAILCHIMP_API_KEY=
MAILCHIMP_AUDIENCE_ID=
MAILCHIMP_SERVER_PREFIX=

# Optional: Base URL for GitHub Pages if applicable (trailing slash)
NUXT_APP_BASE_URL=
