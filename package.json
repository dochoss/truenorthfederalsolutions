{
  "private": true,
  "scripts": {
    "generate-guides": "node ./scripts/generate-guides.js",
    "prebuild": "npm run generate-guides",
    "pregenerate": "npm run generate-guides",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@nuxt/image": "^1.10.0",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@tailwindcss/forms": "^0.5.10",
    "nuxt": "^3.17.4",
    "nuxt-headlessui": "^1.2.0",
    "nuxt-icon": "^0.6.10"
  }
}
