{
  "private": true,
  "scripts": {
    "generate-guides": "node ./scripts/generate-guides.js",
    "prebuild": "npm run generate-guides",
    "pregenerate": "npm run generate-guides",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start-with-api": "concurrently \"npm run dev\" \"cd api && func start\""
  },
  "dependencies": {
    "@microsoft/clarity": "^1.0.2",
    "zod": "^4.3.4"
  },
  "devDependencies": {
    "@iconify-json/heroicons": "^1.2.3",
    "@iconify-json/lucide": "^1.2.82",
    "@iconify-json/simple-icons": "^1.2.64",
    "@nuxt/image": "^2.0.0",
    "@nuxt/ui": "^4.3.0",
    "@types/node": "^25.0.3",
    "concurrently": "^9.2.1",
    "nuxt": "^4.0.0",
    "tailwindcss": "^4.1.18"
  }
}
